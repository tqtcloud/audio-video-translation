# 音频视频翻译系统依赖

# 核心框架
pydantic>=2.0.0
python-dotenv>=1.0.0

# 音频处理
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0
pydub>=0.25.0

# 机器学习和数据处理
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.0.0

# API客户端
openai>=1.0.0
requests>=2.28.0

# WebSocket客户端 (用于火山云服务)
websockets>=15.0.0

# 火山云SDK (可选，如果有官方SDK)
# volcengine-python-sdk>=1.0.0

# 加密和认证
cryptography>=41.0.0

# JSON和数据序列化
orjson>=3.9.0

# 文本处理和质量评估
textstat>=0.7.0
pyphen>=0.14.0
regex>=2023.0.0

# 测试框架
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
unittest-mock>=1.0.1

# 系统监控和性能
psutil>=5.9.0
memory-profiler>=0.61.0

# 并发处理
joblib>=1.2.0
concurrent-futures>=3.1.1

# 日期时间处理
python-dateutil>=2.8.0

# 配置管理
PyYAML>=6.0
configparser>=5.3.0

# HTTP客户端增强
httpx>=0.24.0
aiohttp>=3.8.0

# 日志处理
loguru>=0.7.0
structlog>=23.0.0

# 文件处理
pathlib2>=2.3.7
tempfile-enhanced>=1.0.0

# 数据验证和转换
marshmallow>=3.20.0
cerberus>=1.3.4

# 音频格式转换
ffmpeg-python>=0.2.0

# 音频分析 (可选)
# essentia>=2.1b6.dev609  # 可选，用于高级音频分析
# pyworld>=0.3.0          # 可选，用于音pitch分析
# praat-parselmouth>=0.4.0  # 可选，用于语音分析

# 视频处理 (系统依赖)
# FFmpeg - 需要单独安装
# espeak - 可选，用于语音合成测试

# 开发工具 (可选)
# black>=22.0.0
# flake8>=5.0.0
# mypy>=1.0.0
# isort>=5.10.0
# pre-commit>=3.0.0

# 文档生成 (可选)
# sphinx>=7.0.0
# sphinx-rtd-theme>=1.3.0

# 部署和打包 (可选)
# gunicorn>=21.0.0
# uvicorn>=0.23.0
# docker>=6.0.0